s="Hello World"

def AND(string):
    result=""
    for c in string:
        result+=chr(ord(c)&127)
    return result

def XOR(string):
    result=""
    for c in string:
        result+=chr(ord(c)^127)
    return result

print("AND Result: ",AND(s))
print("XOR Result: ",XOR(s))
